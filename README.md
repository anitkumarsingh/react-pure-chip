### Chip with pure ReactJS

#### How to run the app
Install the dependencies with the following command
npm i or npm install

Running application:-
just type npm start, it will start development server and its open automatically in browser

